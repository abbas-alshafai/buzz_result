name: buzz_result
description: A simple model for success/failure results
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"


dependencies:
  flutter:
    sdk: flutter

  buzz_logger:
    git:
      ref: master
      url: https://github.com/abbas-alshafai/buzz_logger


  buzz_utils: ^0.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
